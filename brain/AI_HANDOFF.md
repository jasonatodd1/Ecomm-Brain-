# AI Handoff — Brain Project

> Paste this entire document into a fresh Claude conversation to pick up where the previous one left off. Verify-flagged items (`[VERIFY]`) are values the AI could not confirm from code/Supabase alone and should be re-checked by the operator.

Last updated: 2026-05-27. Reflects meal planner **live on Etsy** (`4512363257`) + 7-photo set + asset v4 + v5 brief.

---

## 1. SYSTEM OVERVIEW

**What this is:** An autonomous end-to-end e-commerce orchestrator called "the brain." It is responsible for everything a digital-product Etsy shop owner does — discover opportunities, research them, design the product, list it, run customer service, and iterate on results — with a human (Jason) acting as strategic overseer rather than operator.

**Shop:** HillwardStudio on Etsy. Digital products only (printable planners, wall art, SVG bundles, etc.). No physical inventory, no ads.

**Goal:** $1,000 / month profit, ad-free. Validation milestones, in order:
1. First sale
2. Break-even (~40–60 sales/mo at current price points)
3. Net profit at the $1k/mo target

**Current approach:** Research → Brief → Product → List → Validate. Each agent in this pipeline is built as a manual script first, validated for output quality, then scheduled. Closed-loop feedback (sales, reviews, customer messages) flows back upstream into scoring weights, prompts, and design heuristics — but the loops aren't connected yet because there are no live listings to feed them.

We are currently at the **Product → List** stage for the first two products. The Research Agent has been validated and produced briefs for both. Discovery and Scoring run autonomously. Design, Listing, Customer Service, and Optimization agents are all not yet built.

---

## 2. SHOP STATE

- **Shop name:** HillwardStudio
- **Shop URL:** `https://www.etsy.com/shop/HillwardStudio` [VERIFY — code does not pin the canonical URL]
- **Seller account:** opened
- **Live listings:** **3.** Meal planner published 2026-05-27; bunny + A5 calendar from 2026-05-21. All backfilled via `npm run seed:listings` + snapshots via `npm run monitor:listings` / `register:meal-planner-listing`.
- **Reviews:** 0 (new shop — explicit risk factor in every brief; still applies)
- **Monitoring status:** built and manually validated. Daily snapshots via `monitor-listings.ts` are operator-triggered for now — cron deferred per principle #7 until 3–5 days of manual runs confirm baseline behavior.

**Live listings:**

| Product | Etsy ID | Price | Opportunity | Brief | First snapshot |
|---|---|---|---|---|---|
| Meal Planner Printable with Grocery List \| One-Page Weekly Meal Plan + Aisle-Grouped Shopping List PDF… | `4512363257` | $2.00 | `a4376656…` (meal planner printable) | `cb213bf4` (v5) | 0 views / 0 favorers @ 2026-05-27 |
| A5 Monthly Calendar Printable with Notes Pages \| Minimalist Undated Planner Inserts… | `4508059444` | $3.49 | `c3fa0a4d…` (planneraddicts Reddit buyer) | `0834bacd` (proceed, 0.72) | 6 views / 0 favorers / 13 tags / `active` @ 2026-05-21 20:41 UTC |
| Vintage Bunny Nursery Wall Art Printable \| Watercolor Rabbit Sketch \| Gender Neutral Baby Room Decor… | `4508704536` | $4.49 | `d7750211…` (nursery wall art printable) | `ea836ab6` (proceed, 0.62) | 4 views / 0 favorers / 12 tags / `active` @ 2026-05-21 20:41 UTC |

URLs: [`hillwardstudio.etsy.com/listing/4508059444`](https://hillwardstudio.etsy.com/listing/4508059444) and [`etsy.com/listing/4508704536`](https://www.etsy.com/listing/4508704536).

---

## 3. TECH STACK

### Etsy Open API v3 — reviews mining (research-v3)
- **`getListingReviews(listing_id, { maxReviews, pageSize })`** — `GET /v3/application/listings/{listing_id}/reviews`. Same `x-api-key: ${KEYSTRING}:${SHARED_SECRET}` auth as search/listing/shop — **no user OAuth scope required** (verified May 2026). Paginates up to ~50 recent reviews per incumbent. 429s logged non-blocking; Retry-After backoff remains backlog. Reviews filtered for buyer-voice signals (≤3 stars or wishlist/complaint language), then Haiku-paraphrased into `differentiation_thesis.buyer_pain_signals` — **verbatim review text never appears in brief output**.

### Data
- **Supabase** (Postgres + RLS). 13 tables in `public` (added `assets` via migration 0007):
  - `signals` (619+ rows) — raw collector output; new type **`trending_now`** from broad Trending Now collector (`source=google_trends_trending_now`)
  - `opportunities` (17+) — scored signals; enriched with white-space triangulation (`gap_*`, `quadrant`, `gap_analysis` jsonb — migration 0009)
  - `decisions_needed` (2) — surfaced for agent action; both `brief_ready`
  - `product_briefs` (5) — Research Agent output
  - `listings` (2) — live Etsy listings (mirror columns extended in migration 0006: `views`, `num_favorers`, `etsy_state`, `tags`, `etsy_last_modified_at`, `last_snapshot_at`)
  - `listings_stats` (2) — **new in migration 0006.** Append-only time series of every snapshot. Includes `raw` jsonb with the full Etsy response for future-proofing. Indexed on `(listing_id, snapshot_at DESC)` and `(snapshot_at DESC)`.
  - `assets` (17 for bunny) — **new in migration 0007**, extended migration **0008** adds `artwork_flat` kind. Columns: `kind` (CHECK over 12 values: hero / lifestyle / whats_included / size_grid / lifestyle_detail / **artwork_flat** / source_file / print_variant / master / transparent / ratio_guide / crop_marks_pdf), `listing_id` (FK → `listings.id`, nullable), `product_brief_id` (FK → `product_briefs.id`, nullable), `local_path`, `cdn_url`, `width`, `height`, `source` (CHECK over 8 values: fal_generated / fal_upscaled / fal_ui / render_graphic / resize_print / render_planner / build_bundle / manual_upload), `fal_request_id`, `metadata` (jsonb default `{}`). Indexes on `(listing_id) WHERE NOT NULL`, `(product_brief_id) WHERE NOT NULL`, and composite `(kind, listing_id)`. Bunny: 17 rows (7 listing-photos incl. size_grid + 9 deliverables + 1 source_file); planner 1 asset (source_file PDF).
  - `niche_memory` (22) — learnings keyed by `niche_tag` (`planneraddicts`: 17, `general`: 5)
  - `agent_config` (0) — tunable params per agent (no rows seeded yet; presence-only read in Research Agent)
  - `agent_runs` (7) — every agent execution with cost/status/timings
  - `activity` (300+) — chronological event log; now includes `image.generated` / `image.upscaled` from fal tools, `listing.snapshotted` / `listings.monitor_complete` from the monitor, `asset.built` / `asset.linked` / `asset.batch_registered` from the asset pipeline + registry
  - `cost_log` (1) — spend tracking (under-utilized; LLM and image costs currently logged as `cost.api_call` / image-tool activity events, not aggregated to this table — see TODO)
  - `system_state` (3) — global caps/flags/mode
- **RLS:** enabled on 8 tables; **disabled** on `agent_config`, `agent_runs`, `product_briefs`, `listings_stats`, `assets` (these are all server-write-only, but Supabase advisor flags this — open security item if anon key ever gets exposed)

### Compute
- **Railway** — Node 20, `us-west2`, GitHub auto-deploy from `main`. Single service named `brain` [VERIFY service name]. Currently runs `npm run start` = `collect-trends`. Cron not yet configured.

### Source
- **GitHub** — repo `Ecomm-Brain-`. Main branch auto-deploys to Railway.

### Languages & runtime
- **Node.js 20+**, **TypeScript**, ESM (`"type": "module"`). Run via `tsx` (no build step).
- `typecheck` script: `tsc --noEmit`.

### Key libraries
- `@anthropic-ai/sdk` ^0.96.0
- `@supabase/supabase-js` ^2.45.4
- `@fal-ai/client` ^2.x (image generation + upscaling — added 2026-05-21)
- `dotenv` ^16.4.5
- `puppeteer` ^25.0.4 (PDF + PNG/JPEG rendering)
- `sharp` ^0.34.5 (print-size variant generation + dimension verification/correction in the fal pipeline)
- `tsx` ^4.19.1
- `typescript` ^5.6.2

### Models
- **Claude Opus 4.7** (`claude-opus-4-7`) — Research Agent keyword extraction + brief synthesis. ~$0.25 per research run.
- **Claude Haiku 4.5** — Reddit intent classification (`src/lib/classify-intent.ts`). ~$0.001 per classification.
- **Claude Sonnet 4.6** — reserved for future "routine work" per PRINCIPLES.md, not yet used in code.
- **FLUX.2 Pro** (`fal-ai/flux-2-pro`, `fal-ai/flux-2-pro/edit`) — text-to-image and image-to-image (multi-reference, up to 9 refs). ~$0.075 for a 1728×2304 image (4MP). Used by `src/tools/generate-image.ts`.
- **Clarity Upscaler** (`fal-ai/clarity-upscaler`) — diffusion-based upscaler with faithful-upscale tuning baked in (`creativity=0.1`, `resemblance=1.0`). ~$0.10 per upscale (compute-time billed, placeholder estimate). Used by `src/tools/upscale-image.ts`. Fallback for pixel-faithful: `fal-ai/aura-sr` (deterministic GAN, fixed 4x).

### External APIs
- **SerpApi** — Google Trends seed collector (`google_trends` TIMESERIES) + **Trending Now broad collector** (`google_trends_trending_now`, 15 categories, hours=168).
- **Reddit** OAuth — collector for 7 subreddits.
- **Etsy Open API v3** — public listing search + shop enrichment. Auth: `x-api-key: ${KEYSTRING}:${SHARED_SECRET}` (Feb 9, 2026 format change). Etsy Developer App `HillwardStudio Internal` is approved.
- **fal.ai** — hosted FLUX.2 Pro + Clarity Upscaler. Auth via `FAL_KEY` env var. Per-call cost; no monthly fee.

### Environment variables (`.env.local` + Railway Variables)

```
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
SERPAPI_KEY=
ANTHROPIC_API_KEY=
ETSY_API_KEYSTRING=
ETSY_SHARED_SECRET=
FAL_KEY=
```

All seven are required by at least one job; missing vars throw at module load with a clear error. **NB:** `FAL_KEY` not yet added to Railway Variables (verify before any deployed job tries to import `src/lib/fal.ts`).

### MCPs connected in Cursor
- Supabase (read + execute SQL)
- Railway (needs `mcp_auth` re-trigger when stale)
- Vercel (for the future dashboard — no Vercel project yet)

---

## 4. CODEBASE MAP

Root: `brain/`. All paths below are relative to that.

### `src/agents/research/` — Research Agent (v3.2, multi-wedge differentiation)
- **`index.ts`** — orchestrator (`agent_version='research-v3.2'`). Steps 1–6.6 unchanged. **Step 3 niche_tag resolver** (updated 2026-06-02): resolves the `niche_memory` tag with this priority — (1) if `source === 'reddit'` and `context.subreddit` is non-empty, use `subreddit` directly (existing Reddit behaviour, unchanged); (2) else take the first non-empty of `context.niche_tag` / `context.seed_key` / `context.primary_keyword` / `context.opportunity_name` and pass through `slugifyNicheTag()` (lowercase, strip non-alphanumeric/space/underscore, trim, spaces→underscores) — this ensures opportunity_scanner-sourced decisions load their pre-seeded `niche_memory` rows; (3) else fall back to `general`. `slugifyNicheTag` is defined inline in Step 3. Example: `"wedding day timeline template"` → `"wedding_day_timeline_template"`. **Step 6.7** runs `runIncumbentIntel` with a Haiku **relevance filter** on a combined-keyword candidate pool (top-20, expanding to top-30 then top-40 until 3 same-niche kept), then review-mines + extracts product features only on the filtered set. Step 7.4 hydrates `differentiation_thesis.relevance_filter` server-side post-synthesis (deterministic, no need to roundtrip through Opus). Validates `differentiation_thesis` required fields + (v3.2) `wedges[]` shape — every wedge must have a valid `type` and `grounding`, claim, and supporting_evidence. `warnThesisMisalignment()` logs when `description.why_this_one` diverges from thesis. SEO-gap (`listing.competitive_landscape`) is unchanged — top-by-favorers per keyword, no filtering. **Synthesis prompt sanitizer** (added 2026-06-02): `synthesisPrompt` is passed through `sanitizeJsonbDeep()` before the Anthropic API call to strip lone UTF-16 surrogate characters that Anthropic's API rejects as invalid JSON (source: LLM outputs from Haiku buyer_pain_signals or scraped Etsy listing copy).
- **`incumbent-intel.ts`** — product-gap pipeline. `selectRelevantIncumbents()` builds a favorers-ordered cross-keyword candidate pool, classifies each via `classifyIncumbentRelevance()` (Haiku, INCLUDE/EXCLUDE prompt for cross-niche generalization), expands the pool incrementally until 3 same-niche are kept or `MAX_CANDIDATE_POOL_SIZE=40` is exhausted. `runIncumbentIntel()` then fetches reviews + product features per kept incumbent. `RelevanceFilterReport` (pool size, kept/dropped, `pool_exhausted`, `data_thinness`, full classification list) is persisted to brief + raw_research for operator transparency. See `RESEARCH_AGENT.md` §3.
- **`types.ts`** — v3.2 adds `WedgeType`, `WedgeGrounding`, `DifferentiationWedge`, and `DifferentiationThesis.wedges?`. v3.1 adds `RelevanceClassification`, `RelevanceFilterReport`, `IncumbentOffering.relevance_reason`, `DifferentiationThesis.relevance_filter`.
- **`prompts.ts`** — `buildSynthesisPrompt` surfaces relevance filter metadata + drop reasons to Opus, and requires `our_differentiation` to prefix-tag itself "(buyer-voice-backed:" vs "(incumbent-inferred:" so grounding source is never hidden.
- **`competitive.ts`** — unchanged SEO-gap engine (shared with white-space + Listing Agent). NOT used for product-gap selection in v3.1.
- **`render-markdown.ts`** — renders **Relevance Filter** subsection (pool stats + drop reasons) + relevance_reason per kept incumbent.

### `src/agents/listing/` — Listing Agent (v1, Etsy package generator)
- **`index.ts`** — `generateListingPackage(briefId, { store='etsy', listingId? })`. Orchestrator: creates `agent_runs` row (`agent_name='listing'`) → loads brief from `product_briefs` → resolves listing UUID (operator `--listing-id` override; else looks up via `decisions_needed.context.opportunity_id` → `listings.opportunity_id`; null → new-publish path) → loads assets (`OR` join on `listing_id` + `product_brief_id`) → loads niche memory → resolves taxonomy via `mapBreadcrumbToTaxonomyId(brief.listing.etsy_category)` (handles parent-fallback when the brief's leaf doesn't match the live tree — this is what mapped the planner brief's "Planners & Planner Accessories" tail to its parent `Calendars & Planners` id 354, matching the live listing) → fetches taxonomy properties via cached fetcher → renders Etsy plaintext description by calling the Tuning Pass 2 `renderBriefAsEtsyDescription` (single source of truth — same renderer the research brief markdown uses for inline preview) → validates title (≤140, intelligent `|`-boundary truncation) → validates tags (Etsy hard rules only: ≤20 chars, ≤13 count, no exact duplicates — title-overlap is a SOFT scorer penalty per scoring engine Rule 4, not a reject) → maps attributes via `mapAttributes(brief, properties)` from `adapters/etsy.ts` → builds 10-slot image manifest from `brief.listing.image_spec` against the assets registry (matches by `kind`; emits actionable `generation_hint` for misses, including `sharp` cheats when a `master`/`transparent` asset could be the basis of a hero) → picks the toughest incumbent benchmark from `brief.listing.competitive_landscape` (priority: `red_ocean` > `mixed` > `weak_incumbents` > `open_field`; tie-break by highest incumbent median) → scores draft via `scoreEtsyListingSeo()` with `applicable_attribute_count` = non-block-listed properties for the taxonomy and `filled_attribute_count` = mapped attributes (the first caller to populate Rule 8's `attribute_fill_rate`) → if our score percent < incumbent median, runs ONE Opus improvement pass via `improve.ts` on `weak_areas`, re-validates the Opus output through `validateTitle` + `validateTags`, re-scores → assembles `gaps[]` checklist (missing images, title truncated, tag count short, taxonomy fallback, attribute substitutions worth review, SEO still below incumbents after improve, missing shop section) → writes operator-review markdown to `brain/packages/<date>-<brief_id>-etsy.md` → marks `agent_runs` succeeded with full `metadata.package` snapshot for replay. Discrete `activity` rows at every step (`listing.resolved`, `assets.loaded`, `taxonomy.resolved`/`taxonomy.fallback_to_parent`, `taxonomy.properties_fetched`, `description.rendered`, `title.validated`, `tags.validated`, `attributes.mapped`, `image_manifest.built`, `package.scored`, `package.rescored`, `listing.preview_ready`) — all `agent='listing'`.
- **`types.ts`** — `PublishPackage`, `AttributeAssignment` (with substitution audit per value), `AttributeSkip` (with reason: `missing_property` | `no_match` | `free_text_not_mapped`), `ImageSlot` (status `ready`/`missing`, slot 1-10, asset_path or generation_hint), `IncumbentBenchmark` (keyword + median + our_percent + beats), `Store` enum (`'etsy'` only in v1).
- **`adapters/etsy.ts`** — Etsy-specific assembly, pure functions (no DB, no network). `ETSY_RULES` constants (title_max 140, tag_count_exact 13, tag_max_chars 20, images_max 10). `validateTitle(raw)` (preserves leading keyword density by trimming at last `|` that fits). `validateTags(raw, title)` (hard rules + soft overlap counter; never invents tags). `mapAttributes(brief, properties)` walks the live taxonomy property list, routes each to the right descriptor pool from `brief.listing.attribute_intent` (style→Style/Home style, color→Primary color/Secondary color, materials→Material multi, audience+occasion→Room, etc.), block-lists irrelevant properties (TeeShirtSize, Device, Fabric, Custom1-2, Width/Height/Depth/Diameter, Orientation, Aspect ratio, Sustainability, Framing, Can be personalized, Number of pieces included), distinguishes multi-valued vs single-valued, calls `mapSemanticToAllowed` / `mapSemanticToAllowedMany` from `src/lib/attribute-mapping.ts`. `buildImageManifest({ imageSpec, assets, product_name, design_mood })` (matches by kind, picks widest among siblings, emits generation hints).
- **`render-markdown.ts`** — `renderPackageAsMarkdown(pkg, ctx)`. Operator-review markdown. Gaps at the very top (most important — every gap is `- [ ]` checkbox); SEO score with per-rule breakdown table + incumbent-benchmark verdict (✅ beats / ❌ below) + before/after if Opus pass ran; title in fenced block; numbered tag list with char counts; taxonomy with fallback warning if applicable; attributes (substituted ones flagged) + a skip table showing every property the agent considered and why it was skipped; materials free-text list; shop section suggestion; full image manifest with ✅/🟡 badges and generation hints; full Etsy-pasteable description at the bottom in a fenced block.
- **`improve.ts`** — `improveDraft({ title, tags, description, primary_keyword, weak_areas, weak_area_notes, persona_hint }) → { title, tags, description, cost_usd, raw_text }`. Single Opus pass (`claude-opus-4-7`) constrained by a strict system prompt to output ONLY `{ title, tags[], description }` JSON — no commentary, no extra fields. ~$0.10 budget per call (`IMPROVE_COST_USD`). Cap is 1 pass per package per spec; the orchestrator never recurses.

### `src/jobs/` — executable entrypoints
- **`list-package.ts`** — `npm run list:package -- --brief-id=<uuid> [--store=etsy] [--listing-id=<uuid>]`. CLI entry for the Listing Agent. Prints SEO-vs-incumbent verdict, gap checklist, and markdown path. Exits non-zero on agent failure.
- **`collect-trends.ts`** — SerpApi Google Trends seed collector. 10 seed keywords (1 returns no data: `gratitude journal printable`). Writes `interest_score`, `velocity` rows into `signals`.
- **`collect-trending-now.ts`** — `npm run collect:trending-now [--classify-limit=N]`. SerpApi Trending Now broad net: 15 consumer categories, `hours=168`, `geo=US`. Dedupes by query, classifies top-N via Haiku (`classify-trend-relevance.ts`: product vs noise + IP block), writes kept rows as `signals` (`source=google_trends_trending_now`, `metric_type=trending_now`). Category IDs verified at runtime against SerpApi JSON.
- **`collect-reddit.ts`** — Reddit OAuth collector. 7 subreddits, categorized as `buyer` / `seller` / `mixed`. Regex-prefilters buyer-intent posts. For `mixed`/`buyer` subs, sends candidate posts to Haiku 4.5 classifier (`classify-intent.ts`). Stores high-confidence buyer posts in `signals`. Surfaces strong signals as `decisions_needed` rows.
- **`score-opportunities.ts`** — scoring engine. Pass A: Google Trends seeds. Pass B: Reddit buyer posts. **Pass C: Trending Now** (`google_trends_trending_now` / `trending_now` → opportunities with category niche + log-scaled volume/velocity confidence). Upserts to `opportunities` on `name`.
- **`score-whitespace.ts`** — `npm run score:whitespace [--limit=40]`. White-space triangulation on candidate pool (no listing/brief): Etsy search → gap engine → demand×supply matrix → persist on `opportunities`.
- **`run-niche-bakeoff.ts`** — `npm run bakeoff [--run-label=…] [--treatment=baseline|pinterest] [--diff-against=…]`. Curated 9-niche keyword bake-off: **fresh SerpApi Google Trends per keyword** (`bakeoff-demand.ts`: TIMESERIES `today 1-m`, US — same window as `collect-trends.ts`) + Etsy incumbent engagement; Pinterest slot null in baseline → `whitespace-scoring.ts`. Persists to `niche_bakeoff_runs` / `niche_bakeoff_results` (migration 0010) — **does not** touch `signals` or `opportunities`. Raw interest + velocity stored in `gap_analysis` for audit. Reports RAW / BY-NICHE / DIGITAL-PREFERRED + optional diff vs prior run.
- **`shortlist-opportunities.ts`** — `npm run opportunity:shortlist [-- --keywords="a,b,c"]`. **Opportunity scanner v2, PHASE A** (automated, clean); see the dedicated block at the end of §5. Reads `config/opportunity-seed-keywords.txt` (or `--keywords=`), runs the Haiku AI-fit classifier + compliance gate (EXCLUDE craft / AI-art-risk here), writes the Sonnet wedge, and emits the eRank worksheet `config/erank-pulls/{date}.csv` + a Phase-A→Phase-C sidecar `{date}.shortlist.json`. Prints the shortlist + exact eRank instructions. Per-keyword failures log + skip. Seed run ~$0.04, ~1.5 min for 10 keywords.
- **`score-opportunities-from-erank.ts`** — `npm run opportunity:score -- --pulls=config/erank-pulls/{date}.csv`. **Opportunity scanner v2, PHASE C** — ingests the human-filled eRank worksheet + sidecar, computes REAL demand (eRank avg searches) + REAL attackability (eRank top-10 review counts), carries forward Phase A AI-fit/compliance + wedge, applies hard gates, persists to `opportunity_scores` (with `data_source` provenance), and writes the ranked report at `brain/opportunities/{date}.md` (validation section at top). Blank eRank fields → that pillar is UNKNOWN, excluded from the composite (status `incomplete`), never coerced to 0.
- **`research-decision.ts`** — CLI runner for the Research Agent. `--decision-id=<uuid>` arg; default falls back to the planneraddicts seed via `context->>post_url` lookup. Exits process explicitly after 500ms drain.
- **`seed-planneraddicts-decision.ts`** — one-shot seed for the first decision (r/planneraddicts "Looking for something I can't find" post — explicit A5 monthly buyer-intent).
- **`seed-listings.ts`** — `npm run seed:listings`. Idempotent SELECT-then-INSERT by `etsy_listing_id`. Currently seeds the two HillwardStudio listings (`4508059444` A5 planner, `4508704536` bunny print). All other fields left null — `monitor-listings.ts` populates them from Etsy on first run.
- **`monitor-listings.ts`** — `npm run monitor:listings`. Reads all rows from `listings` with non-null `etsy_listing_id`, fetches each via `getListing()` with `mapWithLimit(2, 200ms)` concurrency. Per listing: INSERT into `listings_stats` first (time series is source of truth), then UPDATE `listings` mirror columns. Logs `agent='listing'`, `action='listing.snapshotted'` per success and a run-level `listings.monitor_complete`. Separate warning row (`listing.mirror_update_failed`) if the mirror UPDATE fails after the time-series row already landed — prevents silent divergence. Exits non-zero if any listing failed. Designed for daily cron (deferred until manually validated).
- **`reclassify-reddit-signals.ts`** — backfill script that re-runs the Haiku classifier over existing Reddit signals to upgrade precision without re-collecting.

### `src/tools/`
- **`resize-print-variants.ts`** — `npm run resize:print -- --input=<path> [--product-brief-id=<uuid>] [--listing-id=<uuid>]`. Legacy single-purpose CLI from before the full bundle pipeline shipped — produces only the 5 sized JPG variants (`HillwardStudio-<NamePrefix>-{8x10,11x14,16x20,18x24,24x36}.jpg`). Preserved as a working entry point; delegates all crop coords / sharp ops / VARIANT definitions to the shared `src/lib/print-bundle.ts`. Asset-registry integration (added with migration 0007): inserts one `assets` row per variant with `kind='print_variant'`, `source='resize_print'`, FK columns populated from `--product-brief-id` / `--listing-id`. `--no-register` flag suppresses the writes. Positional-arg form (`tsx src/tools/resize-print-variants.ts <input.png> [output-dir]`) preserved for back-compat. For the complete brief-promised deliverable bundle, use `build-print-bundle.ts` instead.
- **`build-print-bundle.ts`** — `npm run build:bundle -- --master=<path> --product=<slug> [--product-brief-id=<uuid>] [--listing-id=<uuid>]`. Full deliverable orchestrator. Reads a 5008×6680 master and produces every artifact a v2 wall-art brief promises a buyer: `master.jpg` (300 DPI) + `sized/<prefix>-*.jpg` (5 print sizes) + `print-bundle.pdf` (multi-page, one page per size, with crop marks at every trim corner and per-page footer label) + `transparent.png` (background removed via fal birefnet/v2 — preserves full input resolution thanks to `refine_foreground=true`) + `ratio-guide.pdf` (single page US Letter, legend + scale comparison diagram + frame recommendations). Outputs land at `products/<slug>/deliverables/` (gitignored). One `activity` row per deliverable (`agent='product', action='asset.built'`) AND one `assets` row per deliverable (master→`master`, sized JPGs→5×`print_variant`, print-bundle PDF→`crop_marks_pdf`, transparent PNG→`transparent`, ratio-guide→`ratio_guide`, all `source='build_bundle'`) so the future Listing Agent can verify brief claims against on-disk assets via a single SQL query. Dual-mode: CLI or `import { buildPrintBundle }`. Smoke-tested on the bunny 2026-05-21: 41.7s end-to-end, 140.2 MB total, $0.00 fal cost.
- **`generate-image.ts`** — `npm run gen -- --prompt="..." [--kind=hero|lifestyle|...] [--product-brief-id=<uuid>] [--listing-id=<uuid>] [options]`. Dual-mode: CLI for human iteration, or `import { generateImage }` for future agents. Routes to FLUX.2 Pro text-to-image by default, or `flux-pro-edit` (image-to-image, multi-reference, up to 9 refs) when `--ref` is passed. Explicit `--size=WxH` is pinned via sharp post-correction (throws if drift > 1.5×). Auto-named outputs land under `dist/gen/<ts>-<slug>-NN.png`; `--count=N` runs N parallel variants with `seed, seed+1, …`. Every successful run writes an `activity` row (`action='image.generated'`) AND one `assets` row per generated image (kind defaults to `'hero'` since that's the dominant use case; override via `--kind`. `source='fal_generated'`, `cdn_url` = fal CDN URL, dimensions from sharp post-verify, `fal_request_id` populated). Verbose fal `ValidationError` body is surfaced (no more silent `"Unprocessable Entity"`).
- **`upscale-image.ts`** — `npm run upscale -- --input=<path|url> [--kind=master|hero|...] [--product-brief-id=<uuid>] [--listing-id=<uuid>] [options]`. Same dual-mode pattern: CLI or `import { upscaleImage }`. Defaults to Clarity Upscaler with faithful-upscale tuning (`creativity=0.1`, `resemblance=1.0`, conservative against Clarity's defaults of 0.35/0.6). Accepts `--scale=N` or `--size=WxH` (size requires local input so dims can be read; computes scale + sharp-corrects to exact target). Smoke-tested 2026-05-21: 1728×2304 bunny → 5008×6680 in 70s, $0.10. Activity row written under `action='image.upscaled'`, AND one `assets` row written with `kind='master'` (default; override via `--kind`), `source='fal_upscaled'`, `cdn_url`/`width`/`height`/`fal_request_id` all populated.
- **`link-asset.ts`** — `npm run link:asset -- --kind=<...> --path=<...> (--listing-id|--etsy-listing-id|--product-brief-id) [options]`. Manual registry CLI for assets that pre-date the asset pipeline or are produced outside the system. Supports both `--listing-id=<uuid>` and `--etsy-listing-id=<numeric>` (auto-resolved to `listings.id`). Auto-reads `width`/`height` via sharp for raster images (PDFs and non-images leave dims null). Verifies path exists; aborts non-zero if not. Idempotent on `(kind, local_path)` — re-runs print `~ already linked` and exit 0. Writes an `activity` row with `action='asset.linked'` on every successful insert. Used for the 12-row bunny + planner backfill (1×`source_file` per listing + 9 bunny deliverables + 1 bunny `whats_included`). Will continue to handle every hand-uploaded asset until the relevant producer tool gains write-into-assets coverage.

### `src/lib/` (continued — fal infra + asset pipeline)
- **`fal.ts`** — shared fal.ai infrastructure consumed by all tools above AND by future agents. Exports: configured `fal` client (auths via `FAL_KEY`), `MODEL_ALIASES` map (`flux-pro` → `fal-ai/flux-2-pro`, `flux-pro-edit` → `…/edit`, `clarity` → `fal-ai/clarity-upscaler`, `birefnet` → `fal-ai/birefnet/v2`) with `resolveModelId(aliasOrId)` passthrough, cost estimators (`estimateFluxProCost(w,h)`, `estimateClarityUpscaleCost()`, `estimateBirefnetCost()` — returns 0 since fal lists birefnet as free), `resolveReferenceImage(ref)` (uploads local paths to fal CDN, passes URLs through), `downloadImage(url, path)`, `verifyAndCorrectDimensions(path, w, h, {upscaleThreshold=1.5})` (Layer 2/3 dimension guarantee: silent sharp correction for small mismatches, throw for >1.5× drift), `buildAutoOutputPath(prompt, index, ext)` / `buildUpscaleOutputPath(inputPath, scale, ext)` / `indexOutputPath(base, index, total)` for naming, `formatFalValidationError(err)` (extracts `err.body.detail[]` into one human-readable line per offending field), and `removeBackground({input, output, variant, operatingResolution})` (calls birefnet/v2, returns `{outputPath, outputUrl, falRequestId, outputDimensions, variant, operatingResolution, costUsd, durationMs}` — default variant is `'General Use (Dynamic)'` at `'2304x2304'` operating resolution; refines foreground at full input resolution, so a 5008×6680 source returns a 5008×6680 transparent PNG). Throws at module load if `FAL_KEY` is missing.
- **`print-bundle.ts`** — shared asset-pipeline library consumed by `tools/resize-print-variants.ts` and `tools/build-print-bundle.ts`. Pure functions: take a source path + output path, write the deliverable, return structured metadata — NO activity logging (orchestrator does that so it can attach product context). Exports: `PRINT_SIZES` (the canonical 5-size catalog with imperial trim dims + ratio labels + pixel-space crop coords for the 5008×6680 master), `MASTER_WIDTH_PX` / `MASTER_HEIGHT_PX` / `PRINT_DPI` constants, `buildMasterJpg(masterPath, outputPath)` (5008×6680 JPG at 300 DPI density, 4:4:4 chroma), `buildSizedJpgVariants(masterPath, outputDir, namePrefix)` (the 5 print-size JPGs), `buildPrintBundlePdf(variants, outputPath, {productTitle})` (multi-page PDF, one page per variant sized to trim + 0.25" margin all around, embedded JPG fills trim box, 0.25" crop marks drawn at each corner with a 0.0625" gap from the trim edge, per-page footer label), `buildRatioGuidePdf(outputPath, {productTitle})` (US Letter portrait page with title + sub-title + 3-column legend (Size / Ratio / Frame to buy) + visual scale comparison diagram showing nested rectangles for all 5 sizes anchored at a common bottom-left corner + footer note). PDFs built with `pdf-lib`.

### `src/render/`
- **`planner.ts`** — `npm run render:planner`. Headless Puppeteer renders `products/hillward-a5-monthly/template/index.html` to `dist/planner-v1.pdf` at A5 with `preferCSSPageSize`. Waits for `document.fonts.ready` so Google Fonts (`Inter`) embed correctly. Creates `dist/` if missing (gitignored).
- **`meal-planner.ts`** — `npm run build:meal-planner`. Renders one HTML/CSS template (`products/hillward-meal-planner/templates/index.html`) into 4 printable PDFs (`meal-planner-{sun|mon}-{letter|a4}.pdf`) by flipping `data-start` / `data-size` body attributes AND injecting `@page { size: letter | A4; margin: 0 }` via `page.evaluate` before each `page.pdf` call. PDF call uses `preferCSSPageSize: true` + `printBackground: true` only — no `format` / `margin` overrides so CSS is the one source of truth for page geometry. Reuses one Puppeteer browser across all 4 renders (~5s total). Output lands in `products/hillward-meal-planner/deliverables/` (gitignored). Structured-document asset pattern — same shape as `planner.ts` but parameterized for variants. Manual visual iteration: until the `refine:graphic` loop's known flaws are fixed (rewrites assets, critic false-passes wireframes), operator-driven visual review is the iteration loop for structured documents.

**Puppeteer PDF lesson (from v1→v2):** Don't double-declare page geometry. v1 passed Puppeteer `format: 'Letter' | 'A4'` + `margin: { 0, 0, 0, 0 }` AND sized `.page` in mm to match. The PDFs WERE structurally valid (correct MediaBox, %%EOF, 324+ content streams; opened from `open -a Preview` and via AppleScript) but the dual declaration produced subtle conflicts in some macOS PDF reader code paths. Fix: inject a per-render `<style>@page { size: <variant>; margin: 0 }</style>` and call `page.pdf({ path, printBackground: true, preferCSSPageSize: true })`. One source of truth → cleaner output, opens reliably everywhere. Apply this pattern to any future per-variant PDF rendering.

- **`meal-planner-photos.ts`** — `npm run render:meal-planner-photos`. Same template as `meal-planner.ts`, but produces 3 PNG **listing photos** instead of customer-facing PDFs:
  - `pdf-preview.png` (kind=`artwork_flat`, 2550×3300 @ 300dpi) — canonical mon-letter / Letter page rendered via `page.screenshot()` at `deviceScaleFactor: 3.125`. Doubles as (a) Etsy listing photo and (b) fal compositing input.
  - `detail-aisle-headers.png` (kind=`lifestyle_detail`) — tight crop of the `.grocery` element. Computed by reading the bounding rect via `page.evaluate(getBoundingClientRect)` on the high-res render and `sharp.extract({ left, top, width, height })`. Add a small left-context strip in CSS-pixels (×dpr to convert to device-pixels) when you want adjacent context visible.
  - `whats-included.png` (kind=`whats_included`, 3000×3000) — 2×2 composite of all 4 SKUs. Each SKU renders as a base64 PNG thumbnail, then embedded in a standalone HTML grid that Puppeteer captures via `handle.screenshot()` on the `.composite` root. CSS lesson: when constraining an `<img>` inside a CSS-grid cell, set an EXPLICIT pixel height on the wrapper rather than relying on `flex:1 + max-height:100%` on the img — the latter is fragile in Chromium when the parent inherits intrinsic content-sized height from the natural image. Use `grid-template-rows: auto auto` + fixed `.thumb-wrap { height: <px> }` + `.thumb { max-width: 100%; max-height: 100%; object-fit: contain }`.

**Listing-photo split for structured-document products (bunny precedent, meal planner application):** Programmatic technicals (product flat at print resolution, tight detail crops of differentiating features, what's-included grid) go through Puppeteer + sharp from the same source template. Lifestyle composites (page-in-hand at grocery store, planner on kitchen counter, etc.) are out of scope for Cursor — those go through fal UI by the operator using the programmatic `pdf-preview.png` as compositing input. The split mirrors what was done for the bunny (programmatic size-guide + artwork-flat + whats-included via Cursor; lifestyle-* JPGs via fal UI). Code-generated assets are deterministic + crisp + cheap to regenerate when the underlying template changes; fal composites are higher-touch and stay manual.

**Listing photo naming convention (meal planner, 2026-05-27):** Files live in `products/<slug>/listing-photos/`. Numeric prefix sets Etsy display order: `meal-planner-NN-{role}.{ext}` — slots 01–04 fal UI lifestyle (hero + 3 lifestyle), slots 05–07 programmatic (pdf-preview, detail-aisle-headers, whats-included). Register via `link:asset` with `kind=hero` (01), `kind=lifestyle` (02–04), `kind=artwork_flat` (05), `kind=lifestyle_detail` (06), `kind=whats_included` (07). `source=fal_ui` for operator-composited photos, `source=render_planner` for programmatic renders (`npm run render:meal-planner-photos`). Store `display_order` + `role` in asset `metadata` JSON for downstream Listing Agent ordering.

**Programmatic listing photo conventions (structured-document products):** Three renders from the same HTML/CSS template via `meal-planner-photos.ts`: (1) full-page screenshot at 300dpi (`deviceScaleFactor: 3.125` → 2550×3300 Letter) for the product-flat shot; (2) DOM-bounds crop via `sharp.extract()` on the grocery section for structural differentiation; (3) 2×2 SKU composite built from base64-embedded thumbnails in a standalone HTML grid. Combined with fal UI lifestyle composites (01–04) for the full 7-photo Etsy set.
- **`render-graphic.ts`** — `npm run render:graphic <input.html> <output.{png,jpg}>`. Generic screenshot tool for listing assets. Output format is inferred from the file extension: `.jpg`/`.jpeg` → JPEG quality 92, anything else → PNG. Used for the bunny "What's Included" graphic at 2000×2000.

### `src/lib/`
- **`supabase.ts`** — `service_role` Supabase client. Loads `.env.local` at import time; throws on missing creds. `auth.persistSession: false`. Server-side only.
- **`etsy-search.ts`** — Etsy Open API v3 wrapper. Four public functions: `searchEtsy`, `getShop`, `getListing`, **`getListingReviews`** (research-v3 — x-api-key only, no OAuth). Helpers: `sanitizeForJsonb`, `sanitizeJsonbDeep`, `priceToCents`.
- **`etsy-seo-scoring.ts`** — shared SEO scoring engine v1 (Tuning Pass 2). `scoreEtsyListingSeo(listing: EtsyListingDetails, context: { primary_keyword?, niche_tag?, applicable_attribute_count?, filled_attribute_count?, ai_signature_detected?, ai_disclosure_flag? }) → SeoScore`. Pure deterministic function: no DB, no network, no LLM. Same input → same output, callable in bulk (10 results × N keywords per brief). 10 v1 rules — 8 always evaluated (`title_length`, `title_keyword_placement`, `tag_count`, `tag_quality`, `description_length`, `description_keyword_in_preview`, `description_scannable_structure`, `shop_section_assigned`), 2 conditional (`attribute_fill_rate` needs taxonomy `applicable_attribute_count` — future Listing Agent will populate this; `ai_disclosure_compliance` needs `ai_signature_detected` — future signature-detection infra will populate). Returns `{ total, max, percent, weak_areas (sorted desc by max−score), detailed_breakdown[ruleKey] = { score, max, note }, version }`. Exported `SCORER_VERSION = 'v1'`. Consumed today by `competitive.ts`; will also be consumed by the future Listing Agent as a pre-publish quality gate + post-publish drift monitor per `COMPETITIVE_SEO_SCORING.md` §5.
- **`concurrency.ts`** — `mapWithLimit(items, limit, staggerMs, fn)`. Async pool with bounded concurrency and per-task stagger. Etsy uses `limit=2, staggerMs=200` (~5 req/sec sustained, well under Etsy's 10 req/sec ceiling). Preserves input order.
- **`log.ts`** — `log({ agent, action, description, severity?, metadata? })`. Writes to stdout (for Railway log tail) **and** the `activity` table. On insert failure, prints `[ACTIVITY_LOG_FAILED]` to stderr — never swallows the error. Agents enum: `intel | product | listing | customer_service | orchestrator | system`.
- **`classify-trend-relevance.ts`** — Haiku gate for Trending Now queries. PRODUCT vs NOISE + hard IP block. Returns `{ verdict, drop_reason, classification, reasoning }`.
- **`classify-intent.ts`** — Haiku 4.5 classifier for Reddit posts. Returns `{ intent: 'buyer' | 'seller' | 'other', confidence, reasoning }`. Used by `collect-reddit.ts`.
- **`assets.ts`** — shared writer for the `assets` table (migration 0007). Exports `insertAsset(input)` (soft-fail — prints `[ASSET_INSERT_FAILED]` to stderr and returns null instead of throwing, so a registry insert failure never aborts a tool whose file is already on disk), `findAssetByPath(kind, path)` (used by `link:asset` for idempotency — throws on DB errors), and `ASSET_KINDS` / `ASSET_SOURCES` arrays plus matching `AssetKind` / `AssetSource` TS union types kept in sync with the migration's CHECK constraints. Consumed by all 4 asset-producing tools plus the manual `link:asset` CLI.
- **`etsy-taxonomy.ts`** — `getTaxonomyNodes()` (fetches `/v3/application/seller-taxonomy/nodes`, file-cached at `dist/.cache/etsy-taxonomy/nodes.json` with 24h TTL + in-memory cache per process) + `getTaxonomyProperties(taxonomyId)` (fetches `…/nodes/{id}/properties` — returns the full property list with `possible_values` per property, file-cached at `dist/.cache/etsy-taxonomy/properties-<id>.json` with 7d TTL + in-memory cache) + `mapBreadcrumbToTaxonomyId(breadcrumb)` (accepts `string` or `string[]`; descends the tree node-by-node with exact → substring → token-overlap matching; when a crumb doesn't match any child, returns the parent that DID match plus the unmatched tail with `fallback: true` and logs `taxonomy.fallback_to_parent`; throws only when even the first crumb fails to match a root). Auth via the same `x-api-key: ${KEYSTRING}:${SHARED_SECRET}` header as `etsy-search.ts`.
- **`attribute-mapping.ts`** — codification of `LISTING_AGENT_REQUIREMENTS.md` §4 (allowed-value verification). `mapSemanticToAllowed({ descriptors, property })` → `{ value, value_id, was_substituted, reason, matched_from, confidence }` or null (single-valued properties). `mapSemanticToAllowedMany` → `{ matches[], skipped[] }` (multi-valued: Etsy Occasion, Material multi, Art subject, Holiday). Three tiers with explicit fail-to-blank: tier 1 exact normalized equality → tier 2 whole-token containment → tier 3 curated `SEMANTIC_SUBSTITUTIONS` map → null. Never force-picks.
- **`whitespace-scoring.ts`** — white-space triangulation (`scoreWhitespace`). Combines external demand (`confidence_score`) with incumbent engagement (log-normalized median favorers + shop reviews) into `demand_combined`; derives `supply_weakness` from gap classification + SEO median; `white_space_score = demand × supply_weakness`; assigns quadrant (WHITE_SPACE / RED_OCEAN / DEAD_ZONE / MATURE). Pure function, no network.
- **`bakeoff-demand.ts`** — composable external demand for niche bake-off. **Actively fetches** SerpApi Google Trends per keyword (`fetchGoogleTrendDemand` / `fetchGoogleTrendDemandBatch`) — same TIMESERIES params as `collect-trends.ts`; does not read/write `signals`. Baseline: `external = google_demand`; treatment slot: `0.45×google + 0.55×pinterest` when `pinterest_demand` populated.
- **`bakeoff-keywords.ts`** — curated 20-keyword / 9-niche bake-off set with producibility tags and `DIGITAL_PREFERENCE_HURDLE = 0.1`.
- **`gap-score-keyword.ts`** — shared gap scoring for bake-off + whitespace job: Etsy search coherence check, `computeCompetitiveLandscape`, `scoreWhitespace`.
- **`opportunity-scoring.ts`** — pure scoring engine for the opportunity scanner v2 (no DB/network/LLM, same shape as `whitespace-scoring.ts`). `scoreOpportunityErank(input)` → three pillars (Demand / Attackability / AI-fit) from REAL eRank numbers + carried-forward Phase A AI-fit, two hard gates, composite, plus `computeAttackabilityErank` / `scoreAvgSearches` / `computeAiFit` / `aiFitExclusionReason`. **Every threshold/weight is a named, first-pass config constant at the top** (`DEMAND_SEARCHES_LOW/STRONG/CEILING`, `MIN_AVG_SEARCHES`, `REVIEW_TIER_*` + `INVERT_REVIEWS_*`, `SOFT_REVIEW_CEILING`, `FORTRESS_ATTACK_CAP`, attackability weights `W_*`, AI-fit bands `AI_FIT_*` + floor, composite weights `W_ATTACKABILITY/W_DEMAND/W_AI_FIT`). The v1 estimated-sales model (`modelSales`, `REVIEW_TO_SALES_MULTIPLIER`, `MIN_DEMAND_POOL`, `demand_pool`, `youth_signal`) is REMOVED — deprecated as a biased API proxy.
- **`opportunity-pipeline.ts`** — shared plumbing for both scanner phases: lazy Anthropic client + cost tracker, the Haiku incumbent classifier (`assessIncumbents`), the Sonnet wedge writer (`writeWedge`), a light Etsy gather (`gatherPreliminary` — titles + tags only, NO review counts; those are now eRank-sourced), the eRank CSV reader/writer (`parseCsv`/`toCsvRow`/`parseReviewCounts`/`parseNumberCell`), and the Phase-A→Phase-C sidecar IO (`writeSidecar`/`readSidecar`). No new HTTP/auth.

### `products/`
- **`hillward-a5-monthly/template/index.html`** — full 28-page A5 monthly planner template (cover + 24 monthly spreads + 3 notes pages). v3 iteration: SVG-rendered dot grid, larger dot radius, Inter ExtraLight cover, editorial layout, priorities sidebar, system marks, weekend tint. Inline CSS, Google Fonts.
- **`hillward-nursery-bunny/master/bunny 10.png`** — the master illustration. 5008×6680 px [VERIFY], serves as input to `resize:print`.
- **`hillward-nursery-bunny/template/whats-included.html`** — 2000×2000 listing graphic ("What's Included" eyebrow + product summary). Rendered via `render:graphic`.
- **`hillward-meal-planner/templates/index.html`** — single parameterized source for the 4-SKU meal planner. Rendered to 4 PDFs by `meal-planner.ts` via `data-start` (sun/mon) + `data-size` (letter/a4) body attributes. CSS uses custom properties + semantic class names so visual iteration is clean (no magic numbers buried in deep selectors). 4 deliverables registered as `printable_pdf` assets against product_brief_id `cb213bf4` (v5 brief).
- `products/*/dist/` and `products/*/deliverables/` are gitignored — regenerable from templates.

### Other top-level files
- `brain/PRINCIPLES.md` — architecture canon
- `brain/TODO.md` — tactical work list
- `brain/README.md` — Day-1/Day-2 setup guide (older — predates Research Agent, Etsy v3, etc.)
- `brain/briefs/` — generated brief markdowns (currently tracked in git; backlog item to switch to a CLI render tool)
- `brain/supabase/migrations/` — `0001_init.sql`, `0002_rls.sql`, `0003_opportunities_unique_name.sql`, `0005_foundation_schemas.sql`, `0006_listings_monitoring.sql`, `0007_assets.sql`, `0008_assets_artwork_flat.sql`, `0009_opportunities_whitespace.sql`, `0010_niche_bakeoff.sql`, `0011_assets_printable_pdf.sql`, `0012_opportunity_scores.sql`, `0013_opportunity_scores_provenance.sql` (adds `data_source` + `incomplete` status; 0004 absent)
- `brain/opportunities/` — ranked opportunity-scan reports, one `{YYYY-MM-DD}.md` per run (see the Opportunity scanner block in §5)
- `brain/config/opportunity-seed-keywords.txt` — seed keywords for `npm run opportunity:shortlist` (one per line, `#` = comment)
- `brain/config/erank-pulls/` — eRank worksheets (`{date}.csv`, human-filled) + Phase-A sidecars (`{date}.shortlist.json`, machine state). The `.csv` is the manual-data entry point; eRank data enters the pipeline ONLY through it.
- `brain/NICHE_BAKEOFF.md` — niche bake-off design (9 niches, baseline vs Pinterest treatment, three report views, digital hurdle, coherent-keyword rule)

---

## 5. PIPELINE STATE

| Agent | Status | Notes |
|---|---|---|
| Discovery — Google Trends collector | **Built + autonomous** | Seed net: 10 printable keywords via `google_trends` TIMESERIES. |
| Discovery — Trending Now broad collector | **Built + first run done** | 15 consumer categories, Haiku product+IP gate, Pass C scoring. First run: 6 kept from 2175 deduped. |
| Discovery — Reddit collector | **Built + autonomous** | OAuth + intent classifier wired. |
| Scoring | **Built; Pass A/B/C + white-space triangulation + niche bake-off** | Seeds + Reddit + Trending Now → opportunities; `score:whitespace --limit=40` gap-scores top-N by demand. **Niche validation bake-off:** `npm run bakeoff` — baseline (fresh Google Trends per keyword + Etsy engagement; Pinterest treatment TBD); neutral `white_space_score` + digital decision-hurdle (≥0.10 margin). v2 baseline fixes missing Google leg (v1 relied on stale/missing pre-existing signals). |
| Opportunity scanner | **v2 two-phase, eRank-in-the-loop (2026-05-28)** | Phase A `npm run opportunity:shortlist` (auto: AI-fit gate + wedge + eRank worksheet) → human fills CSV from eRank → Phase C `npm run opportunity:score -- --pulls=...` (real demand + attackability + composite). Persists `opportunity_scores` (0012+0013, `data_source` provenance), report at `brain/opportunities/{date}.md`. Demand+attackability are eRank-verified; API-derived versions are deprecated as biased (see block below + TODO). |
| Research Agent V3.2 | **Built — multi-wedge differentiation thesis with per-wedge grounding tags** | v3.2 adds `differentiation_thesis.wedges[]` so each wedge carries its own grounding tag (`buyer-voice-backed` / `partial-buyer-voice-backed` / `incumbent-inferred` / `speculative`) plus `supporting_evidence` and (when applicable) `counter_evidence`. Discipline no longer depends on careful prose: future readers see grounding at a glance, wedges that contradict pain themes must list those contradictions. Wedge type taxonomy: workflow / customization / aesthetic / audience / pricing / other. Lead wedge drives `description.hook` + `positioning`; secondary wedges live in `description.why_this_one`. Meal planner v5 brief (`cb213bf4`) — workflow wedge `incumbent-inferred` (page-flipping foil), customization wedge `partial-buyer-voice-backed` (password/inflexible-entry pain; analytics counter-evidence acknowledged). Asset spec unchanged. Re-synthesis-only CLI: `npm run resynth:meal-planner-v5` (Opus partial update, ~$0.18, no Etsy/Haiku). v3.1 retained: Haiku relevance filter on product-gap stage; SEO-gap unchanged; honest `data_thinness` + `pool_exhausted`. v3 retained: load-bearing `differentiation_thesis` (incumbent product features + paraphrased buyer pain signals + `our_differentiation` / `positioning` / `one_line_claim`). Downstream fields (`description.hook/why`, `image_spec`, `product.design.required_elements`) must reflect thesis. See `RESEARCH_AGENT.md`. |
| Design Agent | **Infra built, agent not yet built** | The image-generation + asset-finishing primitives a Design Agent would call all exist and are validated end-to-end. Generation: FLUX.2 Pro text-to-image, FLUX.2 Pro edit for image-to-image. Enlargement: Clarity Upscaler for print-resolution. Finishing: `npm run build:bundle` produces the full v2-brief-promised deliverable set (master JPG + sized JPGs + print-bundle PDF + transparent PNG via birefnet/v2 + ratio-guide PDF). All tools dual-mode — usable as CLIs today and importable as `generateImage` / `upscaleImage` / `buildPrintBundle` / `removeBackground` from an agent tomorrow. Smoke-tested end-to-end: 1728×2304 watercolor bunny → FLUX.2 Pro → 5008×6680 via Clarity → full deliverable bundle in 41.7s ($0.00 fal cost for the birefnet step; ~$0.18 cumulative for gen+upscale). HTML/CSS layouts (planner, listing graphics) are still designed manually in-chat. |
| Listing Agent v1 | **Built (package-gen) + manual publish** | Shipped in the `feat: Listing Agent v1` commit. `npm run list:package -- --brief-id=<uuid>` produces a complete `PublishPackage` + an operator-review markdown at `brain/packages/<date>-<brief_id>-etsy.md`. Live taxonomy/properties fetch from Etsy's `seller-taxonomy/nodes(/properties)` endpoints (file-cached 24h/7d, in-memory per process). §4 attribute mapping codified in `src/lib/attribute-mapping.ts` (three-tier exact → token → curated semantic, null when no acceptable match — never force-picks). Pre-publish SEO gate via `scoreEtsyListingSeo()` against incumbent benchmark from `brief.competitive_landscape`; if below benchmark, ONE Opus improvement pass on `weak_areas` and re-scores. Audit trail: `agent_runs` row per `(brief_id, store)` with full PublishPackage embedded for replay; discrete `activity` rows (`agent='listing'`) at every step. Validated end-to-end against both v2 briefs (bunny 91%, planner 97% — both beat their incumbent benchmarks; Opus never needed to fire). Etsy publish itself is intentionally manual — Phase 2 OAuth + auto-publish gated on N≥5 consecutive clean previews per (store + taxonomy) combo. |
| Listings Monitoring | **Built + manual** | `seed-listings.ts` + `monitor-listings.ts` running locally on demand. Both live listings seeded, first snapshots captured (see §2 for current numbers). Cron deferred per principle #7 — 3–5 days of manual runs first to confirm baseline (state changes, sold_out, tag edits all behave predictably) before scheduling daily on Railway. The migration (0006) and `listings_stats` time series are ready to absorb cron'd snapshots whenever the operator promotes it. |
| Customer Service | **Not built** | Blocked on first sale. |
| Optimization | **Not built** | Blocked on having performance data — but `listings_stats` now starts accumulating that data immediately. After a week of snapshots, even a manual query can answer "views/day per listing" and "favoriting rate," which is the floor of optimization input. |
| Orchestrator (cron) | **Not built** | Currently every job is git-push-driven (Railway redeploys on commit and runs `npm start`). Cron scheduling is on the backlog. `monitor-listings.ts` is the most immediate cron candidate (daily); collectors follow once their scoring outputs are fully trusted. |

**What's validated end-to-end:** Signal collection → scoring → opportunity surfacing → research brief (v2 with structured description + competitive landscape) → image generation → image upscale-to-print → asset bundle (master + sized + transparent + crop-marks + ratio-guide) → asset registry → **Listing Agent package generation** (live taxonomy-aware attribute mapping + SEO scoring + operator-review markdown) → Etsy listing publish (manual paste) → daily snapshot into `listings_stats` with full signal-to-listing FK traceability via `opportunity_id`. Outputs are reproducible, audited (`agent_runs` + `activity`), and cost-tracked.

**What's next:** drive first sale. Run the monitor manually for a few days to baseline. Once a week of data exists, decide whether the lever is more traffic (Pinterest pin sets, Etsy SEO iteration) or conversion (listing photos, copy, pricing). Use the just-shipped Listing Agent on each existing live listing to diff the agent's generated description against what's currently published — if it reads better, paste it over.

### Opportunity scanner v2 — two-phase, eRank-in-the-loop

**Purpose.** Rank candidate niches by where a zero-review, AI-pipeline shop can realistically win. One row of analysis = one candidate keyword.

**Why two phases.** We verified the Etsy public API cannot reproduce on-site organic/best-match order and never surfaces a niche's true high-review incumbents (`sort_on=score` was tested and was strictly worse — Etsy itself documents that API `score` ≠ website relevancy). So API-derived `median_reviews` / demand were a biased proxy — it wrongly scored "budget planner printable" as attackable. v2 sources the two broken signals — **real demand** and **real top-10 incumbent strength** — from a **manual eRank pull** (a ToS-clean licensed tool) via a CSV worksheet, while keeping the automated gates that already work. No scraper / no new paid API / no new vendor.

**PHASE A — `npm run opportunity:shortlist [-- --keywords="a,b,c"]`** (automated). For each seed keyword: light Etsy gather (titles + tags only, NO review counts) → Haiku AI-fit classifier + compliance gate (EXCLUDE craft / AI-art-generation-heavy here, with reason) → Sonnet **wedge** for gate-passers. Emits `config/erank-pulls/{date}.csv` (one row per gate-passing keyword: `keyword`, `wedge` pre-filled; `etsy_avg_searches`, `etsy_competition`, `top10_review_counts`, `notes` blank) + a `{date}.shortlist.json` sidecar (carries the Phase A AI-fit results so Phase C doesn't re-run them). Any API number seen here is PRELIMINARY/UNTRUSTED and never feeds the score. Prints the shortlist + per-keyword eRank instructions.

**[Human step]** Fill the CSV from eRank: Keyword Tool → `etsy_avg_searches` (monthly) + `etsy_competition`; the "average buyer sees in search results" view → `top10_review_counts` (the actual top-10 organic listings, comma-separated); `notes` → "fortress: <shop>" if one shop dominates the top 3. Blank = unknown.

**PHASE C — `npm run opportunity:score -- --pulls=config/erank-pulls/{date}.csv`.** Ingests the filled CSV + sidecar. Three pillars (0-100) + hard gates:
- **Demand** (REAL) — banded map of `etsy_avg_searches` (monthly). **HARD GATE:** `avg_searches < MIN_AVG_SEARCHES` → EXCLUDED ("insufficient demand"). Saturation ratio (`avg_searches/competition`) is reported for tuning (not yet folded into the score).
- **Attackability** (REAL) — weighted avg over the components present: `invert(median_reviews)` + `soft_ratio` (share of top-10 with reviews < `SOFT_REVIEW_CEILING`=500) from the eRank pull, plus carried-forward Haiku `seo_gap` + `specificity_gap`. A **fortress flag** (one shop dominating the top 3, from `notes`) caps it at `FORTRESS_ATTACK_CAP`. `youth_signal` is dropped (its v1 estimated-sales input was removed); the original weights are renormalized over the components present (the UNKNOWN-excluded rule).
- **AI-fit** — carried forward from Phase A (not re-run). **HARD GATE** at Phase A: `AI_FIT < AI_FIT_FLOOR` → EXCLUDED.
- **Composite** (survivors) = `0.45·attackability + 0.30·demand + 0.25·ai_fit`.

**UNKNOWN handling.** Any blank eRank field → that pillar is UNKNOWN, excluded from the composite (status `incomplete`, `data_source='incomplete'`), surfaced in the report. Blanks are NEVER coerced to 0.

**Provenance (`data_source`, migration 0013).** `erank_verified` = demand + attackability came from the pull; `incomplete` = went through Phase C but a pillar was blank; `api_preliminary` = excluded at the Phase A AI-fit gate (no eRank). Protects the labeled-outcome layer — eRank-verified rows are never silently mixed with API numbers.

**Where output lands.** `opportunity_scores` table (0012 + 0013, keyed by `run_date` + `keyword`: sub-scores, raw signals incl. the eRank pull, status/reason, `data_source`, wedge, rationale, model_meta) + a ranked markdown report at `brain/opportunities/{date}.md` with a **Validation** section at the top (before API-run vs after eRank-run for the control keywords), then eRank-verified survivors, Incomplete, and Excluded sections.

**First-pass constants that need tuning** (top of `src/lib/opportunity-scoring.ts`): `DEMAND_SEARCHES_LOW/STRONG/CEILING`, `MIN_AVG_SEARCHES`, `REVIEW_TIER_1/2/3` + `INVERT_REVIEWS_T1..T4`, `SOFT_REVIEW_CEILING`, `FORTRESS_ATTACK_CAP`, attackability weights `W_MEDIAN_REVIEWS/W_SOFT_RATIO/W_SEO_GAP/W_SPECIFICITY_GAP`, AI-fit bands `AI_FIT_*` + `AI_FIT_FLOOR` + `AI_FIT_COMPLIANCE_CAP`, composite weights `W_ATTACKABILITY/W_DEMAND/W_AI_FIT`. LLM cost-reporting prices in `opportunity-pipeline.ts` are approximate.

---

## 6. KEY DECISIONS (top 10, why)

1. **Closed-loop architecture over pipeline architecture.** Every agent reads `niche_memory` before acting and writes learnings back. Avoids re-discovering the same insights every run. See PRINCIPLES principle #1.
2. **Best tool for the job, not the cheapest.** Opus 4.7 for synthesis (~$0.20/run × 5 runs so far ≈ $1) is far cheaper than the cost of a bad brief that leads to a bad product. Haiku 4.5 only for routine classification. See PRINCIPLES principle #6.
3. **All Etsy market stats computed in code, not by the LLM.** `aggregates.ts` computes medians and percentiles deterministically. The LLM's `market_summary` numbers are reconciled (drift logged, computed values always win). LLMs are unreliable at arithmetic over JSON arrays.
4. **Code-based PDF + listing-graphic rendering via Puppeteer + HTML/CSS; AI image generation via fal.ai (FLUX.2 Pro + Clarity).** Two complementary asset pipelines: structured/typographic work (planners, listing graphics) stays in Puppeteer-rendered HTML/CSS because HTML is the design language Claude is strongest in. Illustrative/photographic work (nursery prints, lifestyle mockups) goes through fal.ai because diffusion models are categorically better at it than CSS. Both pipelines write into `dist/gen/` (gitignored) and log to `activity` so downstream agents can locate generated assets uniformly. fal-side defaults are tuned for *faithful* output (Clarity `creativity=0.1`, `resemblance=1.0`) — the system can override when it wants creative reinterpretation; the floor is conservative.
5. **Volume pricing ($3.49 planner, $4.49 nursery print) over premium pricing.** Direct trade-off vs the 0-review reputation deficit. We can't win on social proof, so we win on price-per-value at a sustainable margin. Each brief explicitly cites comparable competitors before locking pricing.
6. **MVP scoping default for first-in-niche products.** A5 planner shipped as 28 pages, A5 only, undated — not 80 pages with 4 sizes and dated 2026/2027. Reduces design surface, exposes real demand signal faster. Expansion (sizes, dated, bundles) is a v2 candidate listed in the brief.
7. **Single-shop, single-marketplace first.** HillwardStudio on Etsy only. Shopify, Pinterest, TikTok all deferred until Etsy is throwing off enough revenue to justify them.
8. **Etsy Open API v3 (free) over scraping or SerpApi Etsy engine.** Initially built a SerpApi-Etsy wrapper assuming the engine existed — it doesn't. Then built an Etsy v3 client with `x-api-key: keystring`; broke when Etsy changed to `keystring:shared_secret` in Feb 2026. Both were resolved, and PRINCIPLES principle #9 was added: verify external dependencies before integrating.
9. **Decision claiming + agent_runs audit trail are mandatory, not optional.** Every Research Agent run atomically claims its decision (UPDATE … WHERE status='open' RETURNING), records start/end/cost/error, and releases on success or failure. No black boxes, no orphaned claims. See PRINCIPLES principle #5.
10. **Manual-first, then automate.** Every new agent ships as a manual CLI script (`npm run research`), gets a few real runs reviewed by the operator, then is considered for cron scheduling. Cron is a reward for trust, not a default. See PRINCIPLES principle #7.

---

## 7. PENDING WORK (verbatim from `brain/TODO.md`)

### Current Focus

#### Drive first sale (both listings live, daily monitor capturing baseline)
- [ ] Run `npm run monitor:listings` daily by hand for 3–5 days before scheduling cron — confirms baseline behavior, surfaces edge cases (state changes, sold_out, tag edits, etc.) per principle #7
- [ ] First sale (validation milestone) — both listings active, awaiting market signal

#### Use the Listing Agent (just shipped) on real briefs
- [x] Bunny listing photos complete (6 fal UI photos + programmatic size guide in `products/hillward-nursery-bunny/listing-photos/`; manifest **5/5 ready**).
- [ ] Generate planner's 5 missing image slots.
- [ ] Apply bunny description reconciliation manually (brief wins on SEO — fix deliverables to 5 JPGs before paste).
- [ ] Decide Phase 2 OAuth + auto-publish gate (N≥5 clean previews approved per taxonomy before `--publish` becomes default).

### Data-Quality Bugs (open)
- _All three fixed in the `fix(data-quality)` commit; see §9 fixed list._

### Backlog (committed, deferred)
- [ ] **Listing Agent Phase 2 — OAuth + auto-publish.** v1 is package-generator-only; Phase 2 wires the OAuth flow + `POST /v3/application/shops/{shop_id}/listings`. Gated on N≥5 consecutive clean previews per (store + taxonomy) per principle #7.
- [ ] **Listing Agent improvements identified during v1 validation:** style-descriptor ordering (so cottagecore wins over vintage for Home style on the bunny), Art subject discovery from product.name (today's hand-built bunny has Art subject=Animal — v1 doesn't reach it), Pattern free-text auto-fill from style descriptors.
- [ ] Drop dead keywords from seed list (digital planner, printable wall art, custom invitation template); investigate or remove gratitude journal printable (SerpApi failure case)
- [ ] `expense.ts` utility for programmatic cost logging (no more raw SQL inserts)
- [ ] Per-provider cost caps with daily limits (runaway-spend guardrail)
- [ ] Model router abstraction (Opus / Sonnet / Haiku / Gemini swap without code changes)
- [ ] Cron scheduling on Railway (after scoring engine validated AND after 3–5 days of manual `monitor:listings` runs validate output — listings monitor is the most immediate cron candidate; collectors next)
- [ ] Scoring formula ceiling: multiple Google Trends keywords hitting confidence 1.000 — lost discrimination at top, needs refactor (higher ceiling, log scale, or different math)
- [ ] Google Trends velocity volatility: keywords can swing from +8% to +494% in one cycle. Need historical tracking and a stability score before trusting single-run velocity
- [ ] LLM cost tracking integration with cost_log table (currently logging cost.api_call activity events but not aggregating)
- [ ] Etsy 429 retry with exponential backoff (concurrency limit eliminated burst 429s; transient ones still possible — add retry honoring Retry-After header)
- [ ] niche_memory confidence-bump mechanism (currently stuck at 0.50 regardless of evidence_count; confidence should grow with re-confirmation)
- [ ] Monitor synthesis token usage to confirm headroom under max_tokens (one earlier run truncated at 4000; watch as briefs grow with richer data)
- [ ] CLI tool to render any brief from Supabase by ID (`npm run brief -- --id=<uuid>`) — replaces committing markdown to git
- [ ] Decide on `brain/briefs/` git tracking strategy (currently committed; canonical lives in Supabase). Consider gitignore + CLI render tool.

### Future (after first sale validation)
- [ ] Product creation agent (design generation via Recraft/Ideogram/Flux, PDF assembly)
- [ ] **Competitive SEO Scoring engine — remaining work after v1.** v1 scorer + Research Agent integration shipped in the `tune research agent (pass 2)` commit (10 rules, deterministic, no LLM; per-keyword classification on every new brief). Two follow-ups remain — both gated on a Listing Agent existing:
  - [ ] Score every `monitor-listings.ts` snapshot and surface week-over-week drift (`COMPETITIVE_SEO_SCORING.md` §5).
  - [ ] Add `etsy_seo_gap` as a new signal type — only when Listing Agent ships and can act on the gap (`COMPETITIVE_SEO_SCORING.md` §6 step 6).
- [ ] **Pinterest adapter for Listing Agent.** Both v2 briefs flag Pinterest as the highest-leverage external traffic source for HillwardStudio. Pin = image + title + description + destination URL + board assignment. Reuses the v1 Etsy adapter pattern (`src/agents/listing/adapters/etsy.ts`).
- [ ] **Listing Agent v1 — DONE (shipped in the `feat: Listing Agent v1` commit).** Etsy package generator + taxonomy-aware attribute mapping + pre-publish SEO gate. Phase 2 (OAuth + auto-publish) deferred per principle #7.
- [ ] Dashboard on Vercel (Supabase Realtime)
- [ ] Orchestrator (cadence-driven job scheduling instead of git-push-driven)
- [ ] Proactive notifications (Telegram bot, daily email digest, weekly strategic brief)
- [ ] Customer service agent
- [ ] Pricing optimization / A/B testing
- [ ] Backup strategy for Supabase

---

## 8. PRINCIPLES

Canonical doc: `brain/PRINCIPLES.md`. Summary of the 10 core architectural principles:

1. **Closed-loop, not pipeline.** Information flows back upstream from sales/reviews/messages.
2. **Human as overseer, not operator.** Autonomy within defined limits; escalate on new categories, prices >$75, refunds, ad spend, low-confidence calls, pattern shifts.
3. **Agents are tunable without code changes.** Prompts, thresholds, weights live in `agent_config`.
4. **Shared memory is first-class.** `niche_memory` read before, written after.
5. **All work is auditable.** Every run → `agent_runs` with cost, confidence, model, time, outcome.
6. **Best tool for the job. Not the cheapest.** Opus for judgment, Haiku for classification, paid APIs over fragile scraping.
7. **Build, validate, automate.** Manual script → quality-validate → cron → feedback loop.
8. **No silent failures.** `[ACTIVITY_LOG_FAILED]` escalation; cost_log captures spend; loud errors, traceable, recoverable.
9. **Verify external dependencies before integrating.** Most wasted iteration in this project came from API assumptions (SerpApi-Etsy engine didn't exist; Etsy auth format changed). Probe before wrapping.
10. **Living docs must actually live.** TODO.md + PRINCIPLES.md are canonical only if kept current. Doc updates land in the same commit as the work.

---

## 9. KNOWN ISSUES

### Fixed (recently)
- **`listings` schema couldn't store live Etsy state** — mirror columns missing, `title` was `NOT NULL` (originally meant for manual drafts), no time-series table for snapshots. Migration `0006_listings_monitoring` added mirror columns + relaxed `title` + created `listings_stats` (append-only with `raw` jsonb for future-proofing) + unique index on `etsy_listing_id` so seed is idempotent.
- **`node_modules/.package-lock.json` still tracked in git** — left over from before `node_modules/` was gitignored. Untracked in commit `3c98373` ("chore: untrack node_modules/.package-lock.json"). File remains on disk; just no longer noisy in `git status`.
- **Clarity `resemblance` range was outdated in `upscale-image.ts` defaults** — file's docstring claimed `0–3`, but fal's current Clarity schema requires `≤ 1`. Default `1.5` caused 422 on first smoke test. Fixed in commit `519bca0`: default lowered to `1.0`, docstring + CLI help corrected. Same commit also introduces `formatFalValidationError` (shared helper) so all fal `ValidationError` 422s now surface `err.body.detail[]` instead of a generic `"Unprocessable Entity"`.
- **`buildAutoOutputPath` produced double-hyphen filenames** when the 40-char slug slice landed mid-word. Fixed in commit `0d1b419` (trim trailing hyphens after slicing). Existing file `2026-05-21-1255-vintage-watercolor-bunny-scalloped-oval--01.png` predates the fix and keeps its name.
- **Railway `node_modules` warning** — root `node_modules/` was being included in Railway build context. Fixed in commit `21bbf8f` ("add root node_modules/ to gitignore"). Now `node_modules/` and `brain/node_modules/` are both ignored at the repo root.
- **Postgres jsonb rejection on wall-art listings** — Etsy returned raw NUL bytes / control chars in some `description` fields, causing `signals.metadata` inserts to fail with `Empty or invalid json`. Fixed via `sanitizeForJsonb` in `brain/src/lib/etsy-search.ts` (commit `1859387`). Extended in Tuning Pass 2 to also strip lone UTF-16 surrogate halves (which Postgres jsonb also rejects, and which appear when seller text with mathematical-bold unicode is sliced mid-pair, or when LLMs emit malformed surrogate sequences).
- **Etsy v3 auth format** — `x-api-key` had to switch from `keystring` to `keystring:shared_secret` per Etsy's Feb 9, 2026 change. Fixed in commit `465690d`.
- **Asset-pipeline deliverables gap (closed in the `expand asset pipeline` commit).** v2 wall-art briefs (and the bunny in particular) promised buyers a master JPG at 300 DPI + multi-page PDF with crop marks + transparent PNG + ratio-guide PDF — none of which the asset pipeline could actually produce. A Listing Agent publishing from those briefs would have had to either fabricate the missing files, silently drop them, or backfill with a separate manual step. Fix: shared `src/lib/print-bundle.ts` library + `src/tools/build-print-bundle.ts` orchestrator (`npm run build:bundle`), backed by a new fal `birefnet` alias + `removeBackground()` helper in `src/lib/fal.ts`. Outputs land at `products/<slug>/deliverables/` (gitignored). Smoke-tested 2026-05-21 against the upscaled bunny: 41.7s end-to-end, 5 deliverables totaling 140.2 MB, $0.00 fal cost (birefnet/v2 is free per fal pricing page). Transparent-PNG quality verified: all 4 corners fully transparent, bunny preserved at full 5008×6680, 85.8% of pixels alpha=0 (consistent with single-subject illustration on removed background).
- **Asset registry — no first-class table for "what assets exist for what listing/brief" (closed in the `feat: asset registry` commit).** Pre-fix, the only way to find a listing's hero image was to `grep` the `activity` table for the right `image.generated` row; the Listing Agent had no way to walk a brief's deliverables list and verify each file exists on disk; and assets generated outside the system (bunny pre-pipeline-era master, planner PDF) had no registry presence at all. Fix: migration `0007_assets.sql` (new `assets` table with kind / source CHECK constraints, listing_id + product_brief_id FKs, width/height int cols, jsonb metadata, indexes for the dominant lookup patterns), shared `src/lib/assets.ts` writer used by all 4 asset producers (`generate-image.ts`, `upscale-image.ts`, `build-print-bundle.ts`, `resize-print-variants.ts`) so every new artifact registers automatically with FK context, and `npm run link:asset` CLI for manual / pre-pipeline rows. Backfill: 12 rows (11 bunny + 1 planner), 0 missing-disk warnings. Idempotency smoke-tested by re-running a backfill — clean no-op on `(kind, local_path)` match.
- **Etsy attribute schemas vary by taxonomy node — agent now handles correctly (closed in the `feat: Listing Agent v1` commit).** The May 21 lesson (Recipient property doesn't exist on Digital Prints; Materials is constrained-vocabulary on planners and rejects free-text "Digital Download" / "PDF") is now codified end-to-end: `src/lib/etsy-taxonomy.ts` fetches per-taxonomy properties live + caches (24h nodes, 7d properties), `src/lib/attribute-mapping.ts` implements §4's blank-over-wrong contract (exact → token → curated semantic → null), and `src/agents/listing/adapters/etsy.ts` routes descriptors by property name with a block-list for irrelevant slots. Validation confirms: on the bunny (taxonomy 2078), Recipient is silently absent so the agent doesn't try to fill it; on the planner (taxonomy 354), Style/Home style/Room/Art subject are all absent and the agent skips them with explicit reasons; Material multi maps "digital download" → "Paper" via semantic substitution; etc.
- **All 3 data-quality bugs (closed in the `fix(data-quality)` commit):**
  - `opportunities.niche` null on every row → `score-opportunities.ts` now writes niche on every upsert (Reddit→subreddit, Trends→'general'). Backfill: 0 → 11.
  - `opportunities.source_count` stuck at 1 → in-memory aggregation via two helpers (`buildTrendsSourceCountIndex`, `buildRedditSourceCountIndex`); Trends count by keyword, Reddit count by `post_id`. Reddit upserts also deduped by `post_id`. Backfill: 0 → 9 rows with source_count > 1.
  - Reddit `post_url` missing from `signals.metadata` → renamed metadata key `url`→`post_url` in `collect-reddit.ts`; `score-opportunities.ts` reads new key with legacy fallback. Backfill: 0 → 16 individual-post signals carry post_url (7 `new_post_count` aggregate rows legitimately remain without).
- **`FAL_KEY` not yet in Railway Variables** — fixed earlier today. Confirmed present in Railway environment alongside the existing 6 required vars.

### Open
- **Listing Agent v1 limitations identified during validation (see backlog for details).** (1) style-descriptor ordering — agent picks first-match across descriptors, so for the bunny brief "vintage" wins over "cottagecore" and lands on `Home style=Victorian` rather than the closer `Country & farmhouse`. (2) Art subject discovery — agent currently skips it on the bunny because `audience_descriptors` doesn't contain "rabbit"/"bunny"/"animal"; live hand-built listing has `Art subject=Animal`. (3) Pattern free-text surfaces as `free_text_not_mapped` rather than auto-filling from top style descriptor. None are blockers — all surfaced transparently in the agent's `gaps[]` and skipped-properties table; operator can patch in seconds at publish time.
- **`monitor-listings.ts` cron is intentionally deferred.** Per principle #7 ("build, validate, automate"), the monitor runs manually for 3–5 days before getting scheduled on Railway. Two snapshots in `listings_stats` so far (both from the same operator-triggered run on 2026-05-21). Once a few daily runs land cleanly, this is the most immediate cron candidate.
- **RLS disabled on 5 tables** — `agent_config`, `agent_runs`, `product_briefs`, `listings_stats`, `assets`. All server-write-only by design (no client-side write paths), but flagged by Supabase advisor. Not a live exposure unless the anon key ever leaks. Decision deferred until dashboard work begins.
- **Scoring formula ceiling saturation** at confidence 1.000 — lost discrimination at the top of the funnel.
- **`agent_config` table is empty.** Research Agent reads it for presence-only; no tunable params are actually externalized yet. First real config row should appear when prompts move out of source.
- **Clarity Upscaler is diffusion-based**, so even at faithful tuning (`creativity=0.1`, `resemblance=1.0`) it can introduce small detail drift on fine illustrations. For pixel-faithful upscaling of clean line/vector-style art, fall back to `--model=fal-ai/aura-sr` (deterministic GAN, fixed 4x). Documented in `upscale-image.ts` docstring; not yet smoke-tested.
- **Other Clarity parameter ranges have not been independently re-verified** against the current fal schema. `creativity` (0.1), `guidance_scale` (4), `num_inference_steps` (20) all passed the recent smoke test, but a stricter combo could reveal more drift from the docstring. Worth cross-checking when next touching that file.

---

## 10. NEXT SESSION PRIORITIES

Tuning Pass 2 + data-quality bug fixes + asset-pipeline expansion + asset registry are all in. The pipeline is end-to-end clean from discovery through scored research, generated/upscaled assets, full deliverable bundles, and queryable asset registry. **The Listing Agent itself is now the next concrete build** — every upstream prerequisite is cleared.

1. **Begin Listing Agent build.** No remaining blockers. Concrete first slice per `LISTING_AGENT_REQUIREMENTS.md`: (a) store-agnostic core in `src/agents/listing/` — claim a brief-ready decision, load brief + assets (single `SELECT * FROM assets WHERE listing_id = ? OR product_brief_id = ?`), run pre-publish quality gate via `scoreEtsyListingSeo()` against the assembled listing fields, walk `brief.product.format.deliverables[]` and verify every claimed file has a matching `assets` row on disk; (b) Etsy adapter — live `getSellerTaxonomyAttributes(taxonomy_id)` fetch + cache so `attribute_intent` (semantic) gets mapped to `attribute_value` (verified against `possible_values`) at publish time, handles digital-listing variant requirements, posts to `/v3/application/shops/{shop_id}/listings` as draft first; (c) preview-first flow — operator reviews N consecutive briefs before "publish" becomes default (threshold deferred; suggest 5 per spec §7); (d) audit trail — every publish writes a `listings` row, a `listing.published` activity row, and updates the brief's decision status `brief_ready → listed`. Pinterest adapter is the deferred next slice; Shopify deferred indefinitely.
2. **Establish first-sale baseline + triage edge cases.** Run `npm run monitor:listings` once per day for 3–5 days. Watch for: (a) views/favorers monotonically increasing or plateauing per listing — informs whether traffic vs. conversion is the bottleneck; (b) `etsy_state` ever becoming non-`active` (sold_out, removed) so the monitor's behavior under that condition is observed before cron; (c) `etsy_last_modified_at` drifting forward unexpectedly (indicates we edited the listing on Etsy and should reconcile the brief). After ~5 clean runs, promote `monitor:listings` to Railway cron (daily). Per principle #7 — manual first.
3. **Pick a first-sale lever based on what the baseline shows.** Three plausible interventions, each cheap: (a) **Pinterest pin sets** — 5–10 SEO-rich pins per listing pointing at the Etsy URL (no fal cost; mostly time); (b) **Etsy SEO iteration** — if views are low, cross-check tags against the brief's `etsy_tags` and the top-seller patterns from the research brief; (c) **listing photo iteration** — use `npm run gen --ref=...` to produce lifestyle/in-context shots that complement the existing flat-art photos. Don't run all three at once — pick the one the baseline data points to so attribution is clean.

Secondary, if time allows: refresh the bunny's actual Etsy listing files (master JPG + sized JPGs + new transparent PNG + new ratio-guide PDF + new print-bundle PDF) from `products/hillward-nursery-bunny/deliverables/` now that the full bundle exists — the current live listing predates the expanded pipeline and is missing the transparent PNG, crop-marked print bundle, and ratio guide that the v2 brief promises. (All these files are now `assets` rows, so the Listing Agent will eventually do this re-upload automatically.)

---

## 11. HOW TO START A NEW CONVERSATION

Paste **this entire document** into a fresh Claude conversation (Cursor, claude.ai, or API), then say:

> I'm continuing work on the brain project (autonomous Etsy orchestrator under HillwardStudio). The handoff document above describes the current state — please confirm you've read it, then tell me what you think the right next move is and why. The repo is at `/Users/jasontodd/Desktop/coding projects/Ecomm Bot/Ecomm-Brain-/brain`. Don't make any changes yet — start by reading `PRINCIPLES.md` and the latest few entries in `TODO.md` to make sure nothing has drifted, then propose the next concrete step.

For shorter pickups (small bug fix, isolated change), you can skip the full handoff and just paste the relevant section (§4 codebase map + the specific files involved).

If the conversation is going to do anything that touches Supabase or Railway: confirm the operator has run the Supabase MCP at least once in the session (it works via service role and shouldn't need re-auth), and re-trigger `mcp_auth` for Railway if it's stale.
